{
  "Type": "Component",
  "Class": "Instruction",
  "Interface": "Hytale.Instruction",
  "$Comment": "Gate combat instructions when an aggressive mob, hostile memory, or locked RTS target is present.",
  "Parameters": {
    "ViewRange": {
      "Value": 24,
      "Description": "Combat awareness range"
    }
  },
  "Content": {
    "Sensor": {
      "Type": "Or",
      "Sensors": [
        {
          "Type": "Mob",
          "Range": { "Compute": "ViewRange" },
          "LockOnTarget": false,
          "Filters": [{ "Type": "NPCGroup", "IncludeGroups": ["Aggressive"] }],
          "Collector": { "Type": "CombatTargets" }
        },
        { "Type": "HasHostileTargetMemory" },
        {
          "Type": "Target",
          "Range": { "Compute": "ViewRange" },
          "TargetSlot": "LockedTarget"
        }
      ]
    }
  }
}
